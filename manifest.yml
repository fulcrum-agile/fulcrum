---
applications:
- name: {customer_name}
  command: bundle exec rake cf:on_first_instance db:migrate && bundle exec puma -e $RAILS_ENV -p $PORT -S ~/puma -C config/puma.rb
  services:
    - {customer_name}_db_production
  memory: 1G
  instances: 1
